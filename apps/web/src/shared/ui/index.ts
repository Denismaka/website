/**
 * FSD's `shared/ui` layer. In this monorepo the components themselves live
 * in `packages/ui` (@cdc/ui) so every app can reuse them — this file is the
 * app-local access point the architecture brief refers to as `shared/ui`.
 */
export * from "@cdc/ui";
